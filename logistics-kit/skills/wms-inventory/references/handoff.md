# wms-inventory hand-off

Hand-off: 운송·운임은 `tms-routing`, 반품 흐름은 `returns-rma`, ERD/마이그레이션은 `logistics-data-model`, 차감 멱등성은 `logistics-idempotency`, 위치 최적화는 `slotting-putaway`, 피킹 전략은 `wave-pick-strategy`, 정확도·cycle count는 `inventory-accuracy`, 자동화는 `wcs-mhe`, 안전재고·EOQ는 `inventory-planning`, recall lot 추적은 `recall-traceability`.

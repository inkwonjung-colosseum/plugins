> [!IMPORTANT]
> - 도메인:
> - 적용 사용자:
> - 관련 문서:
> - 요구사항 ID 접두사: `FUNC-` (기능), `AC-` (수용기준), `DATA-` (데이터), `API-` (API/이벤트), `NFR-` (비기능), `TEST-` (테스트)
> - 작성 원칙: 빈 행을 남기지 말고, 근거가 부족한 항목은 `[TBD]`로 표시한 뒤 미결 사항에 남긴다.

## 0. 문서 메타

- 문서 ID:
- 버전:
- 상태: 초안 / 검토 / 승인 / 폐기
- 작성자:
- 검토자:
- 승인자:
- 최종 갱신:
- 연관 정책서:
- 매핑된 정책 ID (POL/BIZ/RSK/DEC):

## 1. 개요

- 목적:
- 배경:
- 핵심 사용자:
- 핵심 결과:
- 성공 지표 (정책서 BIZ ID 참조):

## 2. 범위

- 제공하는 것:
- 제공하지 않는 것:
- 단계 (정책서 14절 Phase 매핑):

## 3. 사용자 흐름

- 메뉴 / 화면:
- 외부 진입 채널:
- 종료 조건:

### 3.1 [단계명] (FUNC-001)

- 순서:
- 사용자 행동 / 트리거:
- 시스템 동작:
- 사용자에게 보이는 결과:
- 다음 단계:
- 매핑 정책: POL-XXX

## 4. 화면과 입력 항목

### 4.1 [화면 / 영역명]

- 항목:
- 표시 / 입력:
- 필수 여부:
- 기본값:
- 수정 가능 조건:
- 검증 규칙:
- 관련 정책:

## 5. 기능 동작

### 5.1 [액션명] (FUNC-002)

- 대상 채널:
- 사전 조건:
- 사용자 동작 / 트리거:
- 시스템 처리:
- 사용자에게 보이는 결과:
- 실패 / 부분 성공 기준:
- 관련 정책:

## 6. 권한과 데이터 접근

### 6.1 [역할명]

- 가능 행위:
- 접근 가능 데이터:
- 제한 데이터:
- 관련 정책:

## 7. 예외와 메시지

### 7.1 [예외 상황]

- 처리 등급:
- 사용자 메시지 / 결과:
- 시스템 처리:
- 운영 조치:
- 관련 정책:

## 8. 페르소나와 사용자 여정

### 8.1 [페르소나명]

- 역할 / 직무:
- 목표 (왜 이 기능을 쓰는가):
- 주요 니즈 (Jobs To Be Done):
- 통증점 (Pain points — 실제 인용 가능하면 인용):
- 사전 지식 / 도구 친숙도 (초보 / 중급 / 전문가):
- 사용 빈도 / 시간대 / 디바이스:
- 우선순위 (Primary / Secondary):
- 매핑 시나리오: 8.2

### 8.2 사용자 여정 [여정명]

| 단계 | 사용자 행동 | 사용자 생각 | 감정 / 만족도 (1-5) | 터치포인트 | 통증점 | 개선 기회 | 매핑 FUNC |
|---|---|---|---|---|---|---|---|
| 1. 인지 | | | | | | | |
| 2. 진입 | | | | | | | |
| 3. 사용 | | | | | | | |
| 4. 완료 | | | | | | | |
| 5. 회귀 / 후속 | | | | | | | |

## 9. 수용기준 (Acceptance Criteria)

### 9.1 [AC-001] FUNC-XXX 수용기준

- Given (전제):
- When (행위):
- Then (결과):
- 측정 / 관찰 방법:
- 측정 이벤트 (section 16 이벤트 이름 또는 [TBD]):
- 우선순위: P0 (차단) / P1 (중요) / P2 (보강)
- 자동화 가능: 가능 / 불가 / 부분
- 매핑 테스트: TEST-XXX

## 10. 데이터 모델

### 10.1 [엔티티명] (DATA-001)

- 설명:
- 주요 속성 / 필드 (이름·타입·필수·기본값·길이·제약 — NOT NULL / UNIQUE / CHECK / FK):
- 키 (PK·후보키·외래키 대상 엔티티):
- 관계 (1:1 / 1:N / M:N, 대상 엔티티, 카디널리티·옵셔널리티):
- 인덱스 / 조회 패턴 (예상 query, cardinality, 정렬 / range):
- 분할 / 샤딩 / 파티션 키:
- 상태 (정책서 6절 매핑):
- 라이프사이클 (생성 / 갱신 / 종료 / soft delete):
- 보존 / 폐기 기준 (PII 분류·법적 보존 기간):
- 마이그레이션 전략 (택일 또는 결합):
  - [ ] online migration (zero-downtime, dual-write window)
  - [ ] offline migration (window 명시, downtime ms)
  - [ ] dual-write (구·신 동시 기록, cutover 트리거)
  - [ ] backfill (배치 job, idempotent, 검증 query)
  - [ ] feature flag gated (rollout %)
- Cutover 조건 (성공 신호) / Rollback safe checkpoint:

### 10.2 상태 전이도

| 현재 상태 | 트리거 | 다음 상태 | 부수 효과 | 관련 정책 |
|---|---|---|---|---|

### 10.3 ERD / 관계 다이어그램

- 형식: Mermaid `erDiagram` 또는 DBML. 인라인 fenced code로 포함.
- 포함 엔티티:
- 표기 규칙: PK는 `*`, FK는 화살표, M:N은 중간 엔티티 명시.
- 참고: 큰 ERD는 핵심 엔티티만 다이어그램, 부속은 10.1 카드로 분리.

## 11. API / 이벤트 계약

### 11.1 [엔드포인트] (API-001 — 동기 HTTP/gRPC)

- 종류 (HTTP / gRPC):
- 메서드·경로:
- 인증·인가 (스킴·필요 권한·정책서 7절 매핑):
- 요청 스키마 (필드·타입·필수·예시):
- 응답 스키마 (성공 / 실패·status code):
- 에러 코드 (HTTP·도메인 코드·사용자 메시지):
- 멱등성 (Idempotency-Key 헤더·dedup 윈도우):
- 재시도 (어디서·간격·횟수·exponential backoff·jitter):
- Rate limit / 쿼터 (TPS / 분 / 사용자별):
- 페이지네이션 / 필터·정렬:
- 호환성 (버전·deprecation 정책·sunset 헤더):
- 매핑 기능: FUNC-XXX

### 11.2 [이벤트 / 메시지] (API-002 — 비동기 Event/Webhook)

- 종류 (Kafka / SQS / SNS / Webhook):
- 토픽 / 큐 이름·네이밍 컨벤션:
- producer / consumer:
- 페이로드 스키마 (envelope: event_id, type, version, occurred_at, body):
- 키·파티션 전략 (순서 보장 범위):
- 전달 보장 (at-least-once / exactly-once / at-most-once):
- 멱등성 키 (consumer 측 dedup):
- 재시도·DLQ (deadletter queue 위치·alert):
- Schema Registry / 호환성 (backward / forward / full):
- 매핑 기능: FUNC-XXX

## 12. 비기능 요구사항 (NFR)

> [!IMPORTANT]
> 모든 NFR 수치는 구체값 또는 명시적 `[TBD] + 체크리스트 항목`이어야 한다. "빠름", "안전함", "확장 가능" 같은 정성 표현만 있으면 self-review F9 finding으로 처리되고, 검증 가능한 numeric target이 없으면 `NFR-XXX`는 매핑된 `TEST-XXX`를 가질 수 없다.

### 12.1 성능 (NFR-PERF)

- 응답시간 목표 (수치): p50=__ms, p95=__ms, p99=__ms
- 처리량 (수치): __TPS / __RPS / 피크 __x baseline
- 동시 사용자 (수치): 평시 __ / 피크 __
- 부하 한계 / 스파이크 대응 (auto-scale 트리거·cap):
- 측정 위치 (gateway / service / e2e):
- 매핑 TEST: TEST-XXX (성능 시나리오)

### 12.2 가용성·신뢰성 (NFR-AVAIL)

- SLA / SLO (수치): __% (예: 99.9% = 월 43분 다운타임)
- 에러 버짓:
- RTO / RPO (수치): RTO=__분, RPO=__분
- 장애 격리 (회로차단·bulkhead·timeout·deadline):
- 분산 트랜잭션 패턴 (saga / 2PC / outbox):
- 매핑 TEST: TEST-XXX (장애 주입 / chaos)

### 12.3 보안·개인정보 (NFR-SEC)

- 위협 모델 (STRIDE — Spoofing / Tampering / Repudiation / Information disclosure / DoS / Elevation):
- 식별된 위협별 완화 통제:
- 권한·인증 요건 (정책서 7절 매핑·MFA·세션):
- 개인정보 분류 (식별·준식별·민감), 암호화 (전송 TLS·저장 AES·KMS):
- 마스킹·익명화 규칙:
- 감사로그 요건 (대상 행위·보존 기간·접근 권한):
- 보안 테스트 (SAST / DAST / pen test 주기):
- 매핑 TEST: TEST-XXX (보안 시나리오)

### 12.4 접근성·국제화 (NFR-A11Y)

- WCAG 준수 수준 (수치): AA / AAA — 본문 명도비 4.5:1, 큰 글자 3:1
- 폰트 최소 크기 (수치): 본문 __px, 라벨 __px
- 키보드 / 스크린리더 (탭 순서·ARIA 라벨·실시간 영역):
- 모션·애니메이션 (prefers-reduced-motion 대응):
- 다국어 (지원 언어 목록·번역 키 컨벤션·placeholder):
- 로케일 (날짜·통화·숫자 포맷·timezone):
- RTL 레이아웃 (지원 여부·미러링 컴포넌트):
- 접근성 검증 도구 (axe / Lighthouse / 수동 스크린리더):
- 매핑 TEST: TEST-XXX (접근성 시나리오)

### 12.5 운영·확장성 (NFR-OPS)

- 수평·수직 확장 (트리거 지표·한계):
- 배포 전략 (canary / blue-green / rolling, 단계 비율):
- 롤백 전략 (트리거 조건·자동 / 수동·복구 시간):
- Feature flag / kill switch (kill 조건·기본값):
- 데이터 마이그레이션 (online / offline·backfill·dual write·cutover·rollback safe):
- 환경 (dev / staging / prod·구성 차이):
- 매핑 TEST: TEST-XXX (운영·배포·롤백·마이그레이션 검증 시나리오)

## 13. UX writing 가이드

### 13.1 톤 & 보이스

- 톤 (정중 / 친근 / 단호):
- 문장 형식 (능동 우선, 수동 회피·예외):
- 일관 어휘 / 금지어:
- 호칭·존댓말 기준:
- 길이 가이드 (버튼 ≤4단어, 토스트 ≤2문장, 에러 1~2문장):
- 성공/안내 메시지 순서 (사용자 행동 → 결과 → 다음 단계):
- 오류 메시지 순서 (무엇이 일어났는가 → 왜 → 다음에 무엇을 할지 — 세 부분 모두 포함):

### 13.2 메시지 사례

| 상황 | 메시지 (한글) | 메시지 (영문) | 라벨 길이 한계 | 후속 행동 / CTA |
|---|---|---|---|---|
| 성공 | | | | |
| 경고 | | | | |
| 오류 (사용자 입력) | | | | |
| 오류 (시스템) | | | | |
| 빈 상태 | | | | |
| 로딩 | | | | |
| 확인 (파괴적 행위) | | | | |
| 권한 거부 | | | | |

## 14. 디자인 시스템 / 컴포넌트 연계

- 사용 디자인 시스템 / 라이브러리:
- 사용 컴포넌트 (Button, Modal, Table 등):
- 컴포넌트 선택 기준:
  - 입력 → Input / Select / Autocomplete / Combobox
  - 결정 → Button (primary 1개) / Link / Checkbox / Radio / Toggle
  - 알림 → Toast (3초 자동) / Banner (지속) / Modal (차단)
  - 표시 → Table / Card / List / Empty state
  - 진행 → Spinner / Skeleton / Progress bar
- 디자인 토큰 (color / typography / spacing / radius / elevation):
- 토큰 오버라이드 시 정책 (예: 색상 변경 시 12.4 WCAG 명도비 재검증):
- 디자인 파일 링크 (Figma 등):
- 코드 매핑 (Code Connect 등):

## 15. 테스트 시나리오

### 15.1 [TEST-001] AC-XXX 검증

- 목적:
- 유형: happy / edge / negative / 회귀 / 성능 / 보안 / 접근성 / chaos (장애 주입·failover) / data-migration (dry-run·backfill) / contract (소비자·계약 검증)
- 사전 조건 / 테스트 데이터 (실제 값, fixture 위치):
- 단계 (1, 2, 3 ... 명시적):
- 기대 결과 (관찰 가능 형태):
- 우선순위 (P0 / P1 / P2):
- 자동화 계층 (E2E / API / Unit / Contract / 수동만):
- 자동화 위치 (테스트 코드 경로 또는 [TBD]):
- 매핑 AC: AC-XXX
- 매핑 FUNC: FUNC-XXX

### 15.2 회귀 영향 매트릭스

| 변경 ID (FUNC/DATA/API) | 영향받는 기능 (FUNC ID) | 영향받는 AC (AC ID) | 영향받는 TEST (TEST ID) | Blast radius (single / cohort / tenant / all) | 영향 엔티티 수 | 의존 서비스 수 | 위험도 (P0/P1/P2) | 회귀 자동화 여부 |
|---|---|---|---|---|---|---|---|---|

- 각 변경 ID 행은 영향받는 모든 FUNC/AC/TEST를 빠짐없이 나열한다. 누락 시 self-review F10 finding.
- Blast radius는 변경이 실패할 때 동시 영향을 받는 사용자 범위.
- 영향 엔티티 수 / 의존 서비스 수는 정량 영향도 가중치 산출에 사용.

### 15.3 테스트 데이터 / 환경

- 데이터 fixture 위치 (경로 또는 repo 링크 — "staging DB 참고" 같은 표현 금지):
- 테스트 사용자 / 권한 (정책서 7절 역할 매핑):
- 외부 시스템 stub / mock (계약 버전 명시):
- 환경별 데이터 격리 (dev / staging / prod 차이 + 격리 단위 — 테넌트 / 네임스페이스 / 스키마):
- 데이터 정리 절차 (cross-test 오염 방지 — 트리거·실행 주체·검증):

## 16. Observability

### 16.1 로깅·지표·알람

- 로그 레벨 정책 (DEBUG / INFO / WARN / ERROR — 어떤 사건에 어떤 레벨):
- 핵심 비즈니스 이벤트 로그 (이벤트 이름·필드·PII 마스킹·샘플링):
- 비즈니스 지표 (정책서 10절 KPI 매핑 — 측정 대상·산식·집계 주기·retention):
- 시스템 지표 (RED — Rate / Errors / Duration; USE — Utilization / Saturation / Errors):
- 알람 임계값:
  - P0 (page 즉시): 정의·임계·N분 윈도우·noise 방지
  - P1 (영업시간): 정의·임계
  - P2 (티켓): 정의
- 알람 액션 / runbook 링크:
- 대시보드 위치 (URL 또는 [TBD]):
- 추적 (trace id, correlation id 전파 규칙):
- KPI ↔ 가설 매핑: BIZ-002 가설은 어느 비즈 이벤트로 검증되는가 명시

## 17. 출시 체크리스트 (DoR / DoD)

### 17.1 Definition of Ready (개발 착수 전)

- [ ] 모든 AC (P0/P1)가 Given-When-Then으로 작성·검토되었는가
- [ ] 매핑 정책(POL-XXX)·BIZ-KPI·데이터 모델이 확정되었는가
- [ ] 의존 API/이벤트/외부 시스템 계약이 합의되었는가
- [ ] 보안·개인정보 review가 완료되었는가 (12.3)
- [ ] 접근성 WCAG 수준이 결정되었는가 (12.4)
- [ ] NFR 수치 목표가 측정 가능한가 (p99, SLA, etc.)
- [ ] 디자인·UX 사인오프가 끝났는가
- [ ] 테스트 데이터·fixture가 준비되었는가 (15.3)
- [ ] 회귀 영향 매트릭스가 채워졌는가 (15.2 — 모든 영향 FUNC/AC/TEST + blast radius)
- [ ] 가설 측정 이벤트가 매핑되었는가 (BIZ-002 → 16절 이벤트 → TEST-XXX 폐루프)
- [ ] NFR 수치 목표가 모두 TEST와 매핑되었는가 (NFR-PERF/AVAIL/SEC/A11Y → TEST-XXX)

### 17.2 Definition of Done (출시 전)

각 게이트는 책임자(역할 또는 인명)를 가져야 한다. 책임자 누락 시 F10 finding.

| 게이트 | 책임자 |
|---|---|
| [ ] 모든 P0 AC가 검증·자동화되었는가 | |
| [ ] 회귀 테스트가 통과했는가 | |
| [ ] 성능 NFR (p50/p95/p99)이 측정·충족되었는가 | |
| [ ] 보안 검증 (SAST/DAST/STRIDE 통제 매핑 TEST)이 통과했는가 | |
| [ ] 접근성 NFR (WCAG, axe / Lighthouse)이 통과했는가 | |
| [ ] 운영 알람·대시보드·runbook이 배포되었는가 | |
| [ ] 롤백 절차 / kill switch가 검증되었는가 (drill_date / restored_target / evidence_link) | |
| [ ] 데이터 마이그레이션이 dry-run 검증되었는가 | |
| [ ] 가설 (BIZ-002) 측정 이벤트가 발화·집계되는가 | |
| [ ] 사용자 문서·릴리즈 노트가 준비되었는가 | |
| [ ] 이해관계자 (정책서 11절) 사인오프가 끝났는가 | |

## 18. 미결 사항

### 18.1 [항목]

- 구분: 결정 필요 / 문서 보강 필요 / 출처·누락 참고
- 확인할 것:
- 이유:
- 영향받는 ID (FUNC/AC/DATA/API/NFR/TEST):
- 반영 위치:
- 기한:

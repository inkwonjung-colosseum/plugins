interface:
  display_name: "Planning Drafts"
  short_description: "Generate draft planning artifacts such as brief, PRD, user stories, feature spec, and metrics brief."
  brand_color: "#0F766E"
  default_prompt: "Generate the right draft planning artifact suite from this planning context."

policy:
  allow_implicit_invocation: true
